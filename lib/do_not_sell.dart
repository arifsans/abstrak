import 'package:flutter/material.dart';

class DoNotSell extends StatelessWidget {
  const DoNotSell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
